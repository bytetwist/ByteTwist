name: Coverage

on: [push, pull_request]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Generate coverage report
      run: |
        gradle jacocoTestReport
        gradle testCoverageVerification
